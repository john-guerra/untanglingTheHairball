doctype html
html
  head
    title Untangling the Hairball
    link(rel="stylesheet", href="css/reveal.css")
    link(rel="stylesheet", href="css/theme/white.css")
    link(rel="stylesheet", href="css/main.css")
    link(rel="stylesheet", href="css/scrollable.css")
    link(rel="stylesheet", href="css/scrollableHairball.css")
    link(rel="stylesheet", href="https://fonts.googleapis.com/css?family=Oswald|Quattrocento")

    meta(charset='utf-8')
    meta(name='author', content='John Alexis Guerra Gomez')
    meta(name='description', content="Untangling the Hairball, John Alexis Guerra Gómezs openvis 2017 talk")
    meta(name="viewport",content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, minimal-ui")

    style.
      .reveal h1,
      .reveal h2,
      .reveal h3,
      .reveal h4 {
        font-family: 'Oswald', serif;
      }
      .reveal {

        font-family: 'Quattrocento', sans-serif;
      }
  body

    div.reveal
      div.slides

        div.scrollable
          div#networkChart

        section
          h1 Untangling the Hairball
          h3 By John Alexis Guerra Gómez

        section
          section
            h2 I had a network

          section
            h2 Well, actually

          section
            h2 I had a question

          section
            h2 OpenVis 2014, 2015
            ul
              li.fragment Couldn't come
              li.fragment so I followed through Twitter

          section
            h2 Realizations:
            ul
              li.fragment I need to go!
              li.fragment Twitter is OpenVis backchannel
              li.fragment Am I getting it all?

          section
            h2 Who should I be following to get the best out of OpenVis Twitter discussions?

          section
            h2 Well you could follow all the people that mentions #OpenvisConf

            p TODO: show the list

          section
            h2 What about only the ones with many followers?

          section
            ul
              li.fragment Obama
              li.fragment NyTimes
              li.fragment Tufte

          section
            h2 What about only the people followed the most by the people that talk #OpenVisConf?

          section
            h2 Mindblown!
            p todo: add gif

          section
            h2 Let's visualize it!
            ul
              li.fragment Networks!
              li.fragment D3!

          //- section
          //-   h2 Prayed to Saint Bostock


          section.scroll(step="allGraph")
            h3 State of the art netviz
            p.fragment hairball

          section.scroll(step="onlyCommunity")
            h3 Filter 300!
            p.fragment hairball

          section.scroll(step="top300")
            h3 Only influentials!
            p.fragment hairball

          section.scroll(step="clusters")
            h3 Communities
            p.fragment hairball

          section.scroll(step="clusters")
            h2 Back to my question!

          section.scroll(step="fixedAxis")
            h4 Let's fix axis
            p Number of followers overall vs followers in #OpenVisConf

          section.scroll(step="linksOnDemand")
            h4 Links on demand


        //- Section 2 Tasks
        section
          section
            h1 How to untangle the hairball?
            h2 Tasks!

          section
            h2 Ben and Catherine:
            p What are your tasks?
            p What are your users?


          section
            h2 Tamara's Book
            img(src="images/Tamaras_why.png")

          section
            h2 Types of tasks
            ul
              li Overview
              li Query

          section.demo
            h2 Overview
            img(src="images/overview_10k_full_cropped.png")

          section
            h2 Tip1. Filter
            img(src="images/rank_by_relevance1.png")

          section
            h2 Tip2 Select neighborhood
            img(src="images/rank_by_relevance2.png")

          section
            h2 Tip3 Communities
            ul
              li Netclustering.js
              li Force in a box.js

          section
            h2 Netclustering.js
            //- img(src="images/.jpg")
            //- iframe.demo(src="https://cdn.rawgit.com/john-guerra/ecdde32ab4ad91a1a240/raw/2c1a843df8631604a99140ddc9db8ee048624a79/index.html")
            a.tiny(href="https://github.com/john-guerra/netClusteringJs") https://github.com/john-guerra/netClusteringJs

          section
            h2 Force in a box
            //- iframe.demo(src="https://cdn.rawgit.com/john-guerra/14c943d8f198d9f3fef2/raw/320474d468321d00f3241609a125c8d37935474b/index.html")
            a.tiny(href="https://github.com/john-guerra/forceInABox") https://github.com/john-guerra/forceInABox

          //- TODO: Show video of how it works


          section
            h2 Tip4 Navigate into clusters
            img(src="images/jumpIntoCluster_1.png")

          section
            h2 Tip4 Navigate into clusters
            img(src="images/jumpIntoCluster_2.png")

          section
            h2 Tip4 Navigate into clusters
            img(src="images/jumpIntoCluster_3.png")

          section
            h2 Tip4 Navigate into clusters
            img(src="images/jumpIntoCluster_4.png")

          section
            h2 Tip5 Where am I?
            img(src="images/nodeNavigator.png")

          section
            h2 Use different layouts
            //- iframe.demo(src="http://johnguerra.co/viz/influentials/openvisconf/network.html")
            a.tiny(href="http://johnguerra.co/viz/influentials/openvisconf/network.html") http://johnguerra.co/viz/influentials/openvisconf/network.html

          section
            h1 Query
            ul
              li.fragment Tip6 Ego centric views
              li.fragment Tip7 Expand on demand
              li.fragment Tip8 Node pining
              li.fragment Tip9 Filter on attributes

          section
            iframe.demo(src="http://johnguerra.co/viz/networkExplorer/indexfb.html")

        section

          section
            h2 Other network visualizations
            a(href="http://vistorian.net") http://vistorian.net



        section
          section
            h2 Take home message
            ul
              li.fragment Define your tasks
              li.fragment Rank by relevance
              li.fragment Overview or Query?
              li.fragment Beyond node-link
          //- section(data-markdown)
          //-   script(type="text/template").
          //-     ## Take home message
          //-     Do your networks


    script(src="js/head.min.js")
    script(src="js/reveal.js")
    script(src="js/d3.v4.min.js")
    script(src="js/NetworkCanvasChart.js")
    //- script(src="js/netClustering.js")
    script(src="js/scrollable_hairball.js")
    script(src="js/makeScrollable.js")

    script.
      Reveal.initialize({
        // Display controls in the bottom right corner
        controls: true,

        // Display a presentation progress bar
        progress: true,

        // Display the page number of the current slide
        slideNumber: false,

        // Push each slide change to the browser history
        history: true,

        // Enable keyboard shortcuts for navigation
        keyboard: true,

        // Enable the slide overview mode
        overview: true,

        // Vertical centering of slides
        center: true,

        // Enables touch navigation on devices with touch input
        touch: true,

        // Loop the presentation
        loop: false,

        // Change the presentation direction to be RTL
        rtl: false,

        // Randomizes the order of slides each time the presentation loads
        shuffle: false,

        // Turns fragments on and off globally
        fragments: true,

        // Flags if the presentation is running in an embedded mode,
        // i.e. contained within a limited portion of the screen
        embedded: false,

        // Flags if we should show a help overlay when the questionmark
        // key is pressed
        help: true,

        // Flags if speaker notes should be visible to all viewers
        showNotes: false,

        // Number of milliseconds between automatically proceeding to the
        // next slide, disabled when set to 0, this value can be overwritten
        // by using a data-autoslide attribute on your slides
        autoSlide: 0,

        // Stop auto-sliding after user input
        autoSlideStoppable: true,

        // Use this method for navigation when auto-sliding
        autoSlideMethod: Reveal.navigateNext,

        // Enable slide navigation via mouse wheel
        mouseWheel: false,

        // Hides the address bar on mobile devices
        hideAddressBar: true,

        // Opens links in an iframe preview overlay
        previewLinks: false,

        // Transition style
        transition: 'slide', // none/fade/slide/convex/concave/zoom

        // Transition speed
        transitionSpeed: 'default', // default/fast/slow

        // Transition style for full page slide backgrounds
        backgroundTransition: 'fade', // none/fade/slide/convex/concave/zoom

        // Number of slides away from the current that are visible
        viewDistance: 3,

        // Parallax background image
        parallaxBackgroundImage: '', // e.g. "'https://s3.amazonaws.com/hakim-static/reveal-js/reveal-parallax-1.jpg'"

        // Parallax background size
        parallaxBackgroundSize: '', // CSS syntax, e.g. "2100px 900px"

        // Number of pixels to move the parallax background per slide
        // - Calculated automatically unless specified
        // - Set to 0 to disable movement along an axis
        parallaxBackgroundHorizontal: null,
        parallaxBackgroundVertical: null,

        // Options which are passed into marked
        // See https://github.com/chjj/marked#options-1
        markdown: {
          smartypants: true
        },

        dependencies: [
          // Cross-browser shim that fully implements classList - https://github.com/eligrey/classList.js/
          { src: 'lib/js/classList.js', condition: function() { return !document.body.classList; } },

          // Interpret Markdown in <section> elements
          { src: 'plugin/markdown/marked.js', condition: function() { return !!document.querySelector( '[data-markdown]' ); } },
          { src: 'plugin/markdown/markdown.js', condition: function() { return !!document.querySelector( '[data-markdown]' ); } },

          // Syntax highlight for <code> elements
          { src: 'plugin/highlight/highlight.js', async: true, callback: function() { hljs.initHighlightingOnLoad(); } },

          // Zoom in and out with Alt+click
          { src: 'plugin/zoom-js/zoom.js', async: true },

          // Speaker notes
          { src: 'plugin/notes/notes.js', async: true },

          // MathJax
          { src: 'plugin/math/math.js', async: true }
        ],

      });

    //-
    script.
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

      ga('create', 'UA-72531610-1', 'auto');
      ga('send', 'pageview');

